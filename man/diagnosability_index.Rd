% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnosability_index.R
\name{diagnosability_index}
\alias{diagnosability_index}
\title{Diagnosability Index (DI)}
\usage{
diagnosability_index(mu_dilution, alpha = 0.05, sided = c("one", "two"))
}
\arguments{
\item{mu_dilution}{Numeric. Expected signal under dilution:
mu = (cluster_frac * sc_zscore) / (bulk_se * sqrt(n_eff)).}

\item{alpha}{Numeric. Significance level (default 0.05).}

\item{sided}{Character. "one" for one-sided test (default, recommended for
directional alternatives) or "two" for two-sided.}
}
\value{
List containing:
\itemize{
\item di: Diagnosability Index (power), in range 0 to 1
\item critical_value: Critical z-value used
\item alpha: Significance level
\item sided: Test type ("one" or "two")
\item formula: Human-readable formula string
}
}
\description{
Compute statistical power to detect dilution under a one-sided or two-sided test.
}
\details{
DI estimates the probability of correctly detecting dilution if it truly
occurred at the specified effect size (mu_dilution).

One-sided (default, recommended):
\itemize{
\item Critical value: z = qnorm(1 - alpha) = 1.644854 for alpha=0.05
\item Rationale: Dilution alternative is directional by construction
(sign(mu_dilution) = sign(sc_zscore) since cluster_frac > 0)
\item Formula: DI = pnorm(mu - 1.644854)
}

Two-sided (for reference):
\itemize{
\item Critical value: z = qnorm(1 - alpha/2) = 1.959964 for alpha=0.05
\item Formula: DI = pnorm(mu - 1.959964)
}

Power categories:
\itemize{
\item DI < 0.3: Low power (undetectable at this effect size)
\item DI 0.3-0.8: Moderate power (detection possible but unreliable)
\item DI > 0.8: High power (reliable detection)
}
}
\examples{
# DPP9/M8 k=5 tissue-only
diagnosability_index(mu_dilution = 0.130536, alpha = 0.05, sided = "one")
# Returns: di = 0.0650 (very low power; dilution undetectable)

}
