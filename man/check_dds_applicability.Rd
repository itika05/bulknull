% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_dds_applicability.R
\name{check_dds_applicability}
\alias{check_dds_applicability}
\title{Check DDS Applicability: Bulk Must Be Null}
\usage{
check_dds_applicability(
  bulk_fdr,
  null_fdr_threshold = 0.05,
  on_violation = c("error", "warn", "pass")
)
}
\arguments{
\item{bulk_fdr}{Numeric. FDR-adjusted p-value from bulk analysis.}

\item{null_fdr_threshold}{Numeric. FDR threshold for bulk to be considered "null".
Default: 0.05 (standard significance threshold).}

\item{on_violation}{Character. One of:
\itemize{
\item "error": Stop with informative error message (strict mode)
\item "warn": Issue warning but return results (permissive mode)
\item "pass": Return results silently (developer/testing mode)
}}
}
\value{
A list with elements:
\itemize{
\item applicable: Logical. TRUE if bulk_fdr > null_fdr_threshold (NULL), FALSE otherwise.
\item applicability_note: Character string explaining the status.
\item violation_severity: Character. "none", "warning", or "error" depending on
on_violation setting.
\item bulk_fdr: The input FDR value.
\item null_fdr_threshold: The threshold used.
\item call: The matched function call.
}
}
\description{
Validates whether a case is applicable for DDS analysis.

DDS answers the question: "Given a bulk-NULL result and strong single-cell signal,
is the null due to dilution or true absence of effect?"

This function enforces the applicability boundary:
\itemize{
\item \strong{Applicable:} bulk FDR > null_fdr_threshold (non-significant bulk result)
\item \strong{Not Applicable:} bulk FDR <= null_fdr_threshold (significant bulk result)
}

When not applicable, DDS is mathematically valid but answers the wrong question.
}
\examples{
# Case 1: Bulk is non-significant (FDR = 0.15) - APPLICABLE
check_dds_applicability(bulk_fdr = 0.15, null_fdr_threshold = 0.05,
                        on_violation = "warn")

# Case 2: Bulk is significant (FDR = 0.001) - NOT APPLICABLE
check_dds_applicability(bulk_fdr = 0.001, null_fdr_threshold = 0.05,
                        on_violation = "warn")

}
