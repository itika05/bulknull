% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.bulknull.R
\name{plot.bulknull}
\alias{plot.bulknull}
\title{Plot DI Landscape for Study Design}
\usage{
\method{plot}{bulknull}(
  x,
  sc_zscore = NULL,
  condition_fraction = NULL,
  n_cluster = NULL,
  alpha = 0.05,
  sided = c("one", "two"),
  cluster_fraction_range = c(0.001, 1),
  se_range = c(0.001, 0.5),
  grid_points = 50,
  ...
)
}
\arguments{
\item{x}{A bulknull object}

\item{sc_zscore}{numeric; single-cell z-score (same as input to bulknull)}

\item{condition_fraction}{numeric; case fraction in cohort}

\item{n_cluster}{numeric; cells in target cluster}

\item{alpha}{numeric; significance level (default 0.05)}

\item{sided}{character; "one" or "two" (default "one")}

\item{cluster_fraction_range}{numeric vector of length 2; range for y-axis}

\item{se_range}{numeric vector of length 2; range for x-axis (log scale)}

\item{grid_points}{numeric; number of grid points per dimension (default 50)}

\item{...}{Additional arguments (ignored)}
}
\description{
Visualizes the Diagnosability Index as a function of cluster fraction and
bulk measurement precision (SE), marking the current study's position. Base
graphics only.
}
\details{
The landscape shows DI as a 2D function of cluster_fraction and bulk_se.
Higher DI (>0.8) indicates good power; lower DI (<0.3) indicates underpowered.
Contours mark DI = 0.3 and DI = 0.8 boundaries. The study is marked with a
red dot at (bulk_se, cluster_fraction) from the input bulknull object.
}
