% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort_inflation.R
\name{cohort_inflation}
\alias{cohort_inflation}
\title{Cohort Inflation Factor for Target Diagnosability Index}
\usage{
cohort_inflation(
  mu_observed,
  target_di = 0.8,
  r = 1,
  alpha = 0.05,
  sided = c("one", "two")
)
}
\arguments{
\item{mu_observed}{numeric; observed effect size (bulk z-score / sqrt(n_eff_bulk))}

\item{target_di}{numeric; target diagnosability index (0 to 1, default 0.8)}

\item{r}{numeric; relative expression parameter. Default 1 (no adjustment). Used for reference only.}

\item{alpha}{numeric; significance level (default 0.05)}

\item{sided}{character; "one" or "two" for one-sided or two-sided test (default "one")}
}
\value{
A numeric scalar: the required inflation factor (>= 1.0 if target is attainable)
}
\description{
Compute the required inflation in cohort size (and thus reduction in bulk SE)
to achieve a target diagnosability index. The inflation factor is independent of
the pooled cohort size N and the bulk point estimate; it depends only on the
observed and required effect sizes mu.
}
\details{
Identity: required inflation = (mu_required / mu_observed)^2

The relationship between bulk SE and cohort size is:
SE ~ 1 / sqrt(N)

To reduce SE by a factor k, cohort size must increase by k^2. Therefore,
if the current mu is mu_obs and we need mu_req, the inflation is:
inflation = (mu_req / mu_obs)^2

This computation is independent of:
\itemize{
\item The absolute cohort size
\item The bulk point estimate (only mu_obs matters)
\item The cluster fraction or single-cell effect size
}

An inflation of 50x means the cohort must grow 50 times in size.
An inflation of 50 with current N=630 implies new N ~ 31,500.
}
\examples{
inflation <- cohort_inflation(
  mu_observed = 0.130536,
  target_di = 0.8,
  alpha = 0.05,
  sided = "one"
)

cat("Required inflation:", inflation, "x\n")
cat("If current N=630, new N would be:", 630 * inflation, "\n")

}
